import type { FunctionComponent } from "react";

interface LogsSendMailProps {}

const LogsSendMail: FunctionComponent<LogsSendMailProps> = () => {
  return <>Logs Send Mail Page</>;
};

export default LogsSendMail;
